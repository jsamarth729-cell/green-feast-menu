node_modules/
drive-download-20260620T212546Z-3-001/
images_compressed/
*.bat

# local tooling / caches
.claude/
.playwright-mcp/

# Design references, mockups, review screenshots and hand-cut source art.
# All working files — the committed assets live in images/ and images/nobg/.
# Any loose PNG in the repo root is scratch by definition; the real ones are
# never stored here. ("QSR Menu Display.png" alone is ~24 MB.)
/*.png
/*.jpg
/*.jpeg

# Screen 2 mockups + hand-cut source art (same reasoning as above —
# the committed cut-outs live in images/screen2/).
Screen 2/

# Screen 3 mockups + source art (same reasoning as above —
# the committed cut-outs live in images/screen3/). Anchored to the repo root
# (leading slash) — an unanchored "Screen3/" also matched images/screen3/ on
# Windows' case-insensitive filesystem, since the names differ only by a space.
/Screen3/
